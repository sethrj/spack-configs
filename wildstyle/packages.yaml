packages:
  cuda:
    buildable: false
    externals:
    - spec: cuda@11.5.20211118
      prefix: /usr/local/cuda-11.5
  scale-data:
    buildable: false
    externals:
    - spec: scale-data@local
      prefix: /data/scale/data
  all:
      variants: +cuda cuda_arch=70

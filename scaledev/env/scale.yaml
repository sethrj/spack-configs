# This is a Spack Environment file for the new SCALE CI environment.
# It should also work on mac systems as well.
spack:
  # add package specs to the `specs` list
  specs:
  - bison@3.4.2
  - cmake
  - doxygen+graphviz
  - flex@2.6.4
  - gsl
  - hdf5 +fortran +hl ~mpi



  
    # - 'java@14:' # jdk fails to fetch
  - ninja
  - nlohmann-json
  - openblas
  - openmpi@:2.999
  - python +uuid
  - py-h5py@:2.999 ~mpi # compound vlen bug in 2.6-3.1 and 3.3+
  - py-h5sh
  - py-lxml
  - py-jsonschema
  - py-matplotlib
  - py-numpy
  - py-pandas
  - py-pint
  - py-psutil
  - py-sphinx
  - py-pyyaml
  - qt@5.0:5 +gui
  - silo +fortran ~mpi
  - swig@4.0.2-fortran
  view: true
  concretization: together
  packages:
    hdf5:
      version: [1.10.8] # needed for h5py@2 
    hwloc:
      version: [1.11.13] # needed for openmpi 2
    qt:
      variants: ~dbus~examples~gtk~opengl~phonon+shared~sql~ssl~tools~webkit
      version: [5.9.9] # blessed for CI, incompatible with new macos
    all:
      providers:
        blas: [openblas]
        lapack: [openblas]
        mpi: [openmpi]

config:
  install_tree: /gpfs/alpine/proj-shared/csc404/celeritas/spack/opt
  install_path_scheme: '{compiler.name}-{compiler.version}/{name}/{hash:7}'
  module_roots:
    lmod: /gpfs/alpine/proj-shared/csc404/celeritas/spack/lmod
  build_stage:
    - $tempdir/$user/spack-stage
    - $spack/var/spack/stage
  extensions:
    - /sw/sources/facility-spack/summit/share/spack/extensions/spack-olcf
  build_jobs: 6
  db_lock_timeout: 120
  concretizer: original

